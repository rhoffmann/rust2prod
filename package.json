{
  "scripts": {
    "build": "npm run build:styles",
    "build:styles": "npx tailwindcss -i ./templates/tailwind.css -o ./static/styles.css",
    "start": "npm run watch:styles",
    "watch:styles": "npx tailwindcss -i ./templates/tailwind.css -o ./static/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/aspect-ratio": "^0.4.2",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/typography": "^0.5.12",
    "tailwindcss": "^3.4.3"
  }
}